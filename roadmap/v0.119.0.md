# v0.119.0 — OWL propertyChainAxiom, SERVICE Circuit Breaker, and Schema-Aware NL→SPARQL

**Status:** 🔜 Planned

## Summary

v0.119.0 delivers three features from the Platform Maturity roadmap: `owl:propertyChainAxiom`
inference in the OWL-RL Datalog rule set (Feature 5), a federated SERVICE circuit
breaker with Prometheus gauge (Feature 6), and schema-aware NL→SPARQL translation
that includes active vocabulary bundle metadata in the LLM system prompt (Feature 10).
Also fixes the property-path-over-RDF-star gap and extends the OWL-RL regression suite.

## Deliverables

### Feature 5 — `owl:propertyChainAxiom` Inference

- [ ] Implement `owl:propertyChainAxiom` in `src/datalog/builtins.rs` OWL-RL rule set
- [ ] `?x owl:propertyChainAxiom (?p1 ?p2 … ?pn)` compiles to n-step Datalog chain rule
- [ ] Add 10 canonical pg_regress examples:
  - FOAF `foaf:knows` chain (2-hop acquaintance)
  - SKOS `skos:broaderTransitive` closure
  - PROV-O `prov:wasInfluencedBy` derivation chain
  - 7 additional chain scenarios
- [ ] Add propertyChainAxiom test to LUBM CI gate
- [ ] Document in `docs/src/reference/owl-rl.md`

### Feature 6 — SERVICE Circuit Breaker

- [ ] Create `_pg_ripple.federation_circuit_state (endpoint_iri TEXT PRIMARY KEY, state TEXT, last_failure_at TIMESTAMPTZ, failure_count INT)` table
- [ ] Circuit trips to `'open'` after `pg_ripple.federation_circuit_failure_threshold` consecutive failures (default 5)
- [ ] Transitions to `'half_open'` after `pg_ripple.federation_circuit_recovery_secs` seconds (default 60)
- [ ] Returns to `'closed'` on first successful probe in `'half_open'` state
- [ ] Add Prometheus gauge `pg_ripple_federation_circuit_state{endpoint}` (0=closed, 1=open, 2=half_open)
- [ ] Migration: `CREATE TABLE _pg_ripple.federation_circuit_state (...)`

### Property Paths over RDF-star Gap Fix

- [ ] Compile `?x ?p ?y` patterns inside `<<...>>` quote context to `temporal_facts`-style routing
- [ ] Property paths traverse reified edges natively
- [ ] Add 5 pg_regress tests for property paths over RDF-star

### OWL-RL sh:propertyChainAxiom Regression Extension

- [ ] Extend `tests/pg_regress/sql/owl_rl_sameas.sql` with `owl:propertyChainAxiom` cycle-safety tests
- [ ] Verify no infinite loop when a chain contains the derived property itself

### Feature 10 — Schema-Aware NL→SPARQL

- [ ] Add GUC `pg_ripple.nl_sparql_include_bundles BOOLEAN DEFAULT on`
- [ ] When on, `sparql_from_nl()` includes active vocabulary-bundle metadata in LLM system prompt:
  - SKOS preferred labels (`skos:prefLabel`)
  - DCTERMS titles (`dcterms:title`)
  - Schema.org type names (`schema:name`)
  - FOAF names (`foaf:name`)
- [ ] Bundle metadata is fetched via SPI query before each `sparql_from_nl()` call
- [ ] Document grounding improvement in `docs/src/cookbook/nl-to-sparql.md`

### Migration & Release

- [ ] Migration script `sql/pg_ripple--0.118.0--0.119.0.sql`:
  - `CREATE TABLE _pg_ripple.federation_circuit_state (...)`
- [ ] `pg_ripple.control` updated to `default_version = '0.119.0'`
- [ ] `Cargo.toml` and `pg_ripple_http/Cargo.toml` versions bumped to `0.119.0`
- [ ] `COMPATIBLE_EXTENSION_MIN` bumped to `"0.118.0"` in `pg_ripple_http/src/main.rs`
- [ ] `CHANGELOG.md` entry present for `[0.119.0]`

## Exit Criteria

- All checklist items above are ticked
- `cargo clippy --features pg18 -- -D warnings` passes with zero warnings
- `cargo pgrx test pg18` passes
- `cargo pgrx regress pg18` passes (including 10 propertyChainAxiom tests and 5 RDF-star property path tests)
- LUBM CI gate passes with propertyChainAxiom test included
- Circuit breaker state transitions verified in pg_regress
- Migration script `sql/pg_ripple--0.118.0--0.119.0.sql` applies cleanly
- `CHANGELOG.md` entry present for `[0.119.0]`
