# v0.114.0 — A16 Medium: Module Splits and Architecture Debt

**Status:** 🔜 Planned

## Summary

v0.114.0 addresses six medium-severity module-size findings from Assessment 16
(H16-06a, H16-06b, M16-14 through M16-18). Every module exceeding 1,000 LOC is
decomposed into focused sub-modules following the patterns already established in
`src/datalog/` and `src/sparql/`. A CI gate is added to prevent future monolith
growth. An architecture dependency graph is added to `docs/src/architecture.md`.

## Deliverables

### H16-06a — Decompose `src/views/mod.rs` (1,599 LOC)

- [ ] Split into `src/views/{mod.rs, construct.rs, materialise.rs, refresh.rs, dependency.rs}`
- [ ] Each file < 400 LOC
- [ ] All existing public API re-exported from `src/views/mod.rs`
- [ ] pg_regress tests pass unchanged

### H16-06b — Decompose `src/skos.rs` (1,495 LOC)

- [ ] Split into `src/skos/{mod.rs, bundle.rs, inference.rs, export.rs, broader_narrower.rs}`
- [ ] Mirrors the `src/datalog/` and `src/sparql/` sub-module layout
- [ ] All existing public API re-exported from `src/skos/mod.rs`

### M16-14 — Split `src/datalog_api.rs` (1,134 LOC)

- [ ] Split into `src/datalog_api/{mod.rs, parse.rs, validate.rs, explain.rs, conflict.rs}`
- [ ] Each file < 400 LOC

### M16-15 — Split `src/sparql/wcoj.rs` (1,067 LOC)

- [ ] Split into `src/sparql/wcoj/{mod.rs, executor.rs, trie.rs, leapfrog.rs}`

### M16-16 — Split `src/sparql/embedding.rs` (1,144 LOC)

- [ ] Split into `src/sparql/embedding/{mod.rs, index.rs, hybrid.rs, rag.rs}`

### M16-17 — Split `src/shacl/validator.rs` (1,181 LOC)

- [ ] Split by shape kind into `src/shacl/validator/{mod.rs, property.rs, node.rs, sparql.rs, severity.rs}`

### M16-18 — Split `src/citus/mod.rs` (1,366 LOC)

- [ ] Split into `src/citus/{mod.rs, shard_pruning.rs, ddl_hooks.rs, query_rewriting.rs, rebalance.rs}`

### CI Gate — Module Size Enforcement

- [ ] Add CI step that warns at 1,200 LOC and fails at 1,500 LOC per `.rs` file
- [ ] Gate runs on every PR via `scripts/check_module_sizes.sh`
- [ ] Document the policy in `CONTRIBUTING.md`

### Architecture Documentation

- [ ] Add `docs/src/architecture.md` with subsystem dependency graph:
  SKOS→Datalog, OWL-RL→Datalog, NS-RL→embedding+Datalog+hypothetical,
  conflict-detection→SHACL+Datalog, hypothetical→storage

### Migration & Release

- [ ] Migration script `sql/pg_ripple--0.113.0--0.114.0.sql` (comment-only: no schema changes)
- [ ] `pg_ripple.control` updated to `default_version = '0.114.0'`
- [ ] `Cargo.toml` and `pg_ripple_http/Cargo.toml` versions bumped to `0.114.0`
- [ ] `COMPATIBLE_EXTENSION_MIN` bumped to `"0.113.0"` in `pg_ripple_http/src/main.rs`
- [ ] `CHANGELOG.md` entry present for `[0.114.0]`

## Exit Criteria

- All checklist items above are ticked
- `cargo clippy --features pg18 -- -D warnings` passes with zero warnings
- `cargo pgrx test pg18` passes
- `cargo pgrx regress pg18` passes
- CI module-size gate passes (no file > 1,500 LOC)
- Migration script `sql/pg_ripple--0.113.0--0.114.0.sql` applies cleanly
- `CHANGELOG.md` entry present for `[0.114.0]`
