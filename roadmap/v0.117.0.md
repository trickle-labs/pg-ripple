# v0.117.0 — A16 Low-Severity Polish, Tests and Supply-Chain

**Status:** ✅ Released

## Summary

v0.117.0 closes out all low-severity findings from Assessment 16 (L16-01 through
L16-15) and two medium-severity items deferred from earlier releases (M16-12 and
M16-13). The release is focused on supply-chain security, documentation, and
concurrency test coverage — no schema changes, no new user-visible features.

## Deliverables

### L16-01 — Reduce `#[allow(...)]` Suppressions

- [x] Reduce suppressions by 10% (target ≤ 186 from current ~206)
- [x] Annotate remaining suppressions with `// A16-CQ:` comments explaining necessity

### L16-02 — Crash Recovery Documentation

- [x] Add `tests/crash_recovery/README.md` documenting:
  - `pg_ctl -m immediate` invocation pattern
  - `PGDATA` requirements
  - Expected outcomes and recovery verification steps

### L16-03 — Benchmarks Documentation

- [x] Add `benchmarks/README.md` documenting:
  - Interplay between `ci_benchmark.sh`, `pgbench`, and per-subsystem SQL files
  - How to run individual benchmarks locally
  - How to interpret `merge_throughput_history.csv` and `pagerank_throughput_history.csv`

### L16-04 — Replication Worker Doc-Comment

- [x] Add `///` doc-comment block to `pg_ripple_logical_apply_worker_main` in `src/replication.rs`
- [x] Enumerate restart behaviour, crash-loop backoff, and `BGWORKER_RESTART_TIME` constant

### L16-05 — Arrow Flight Ticket Migration Documentation

- [x] Add `docs/src/reference/arrow-flight.md` section explaining:
  - v1→v2 HMAC ticket migration path
  - What constitutes a valid v1 ticket during the transition window
  - How to identify expired v1 tickets

### L16-06 — HTTP Auth Realm Configuration

- [x] Read `PG_RIPPLE_HTTP_AUTH_REALM` env at startup
- [x] Use it in the `Bearer realm=` field of `WWW-Authenticate` header in `check_auth`
- [x] Default to `"pg_ripple"` if env not set
- [x] Document in `pg_ripple_http/README.md`

### L16-07 — SQL Migration List

- [x] Add `sql/INSTALL.md` with numbered sequential migration list from `0.1.0` to current version
- [x] Include checksum verification instructions for each migration script

### L16-08 — Docker Image Tag Pinning

- [x] Pin `pg_ripple` image tag in `docker-compose.yml` to current minor version (not `latest`)
- [x] Document pinning policy in `docker/README.md`

### L16-09/10 — Gitignore Build Artifacts

- [x] Add to `.gitignore`:
  - `clippy_all.txt`
  - `clippy_output.txt`
  - `cargo_check_output.txt`
  - `build_output.txt`
  - `regression.diffs`
  - `sbom_diff.md`
- [x] Remove committed copies of these files from the repository
- [x] Note: Requires `git rm --cached` for already-tracked files

### L16-11 — SBOM Diff Generation Header

- [x] Add `**Generated:** YYYY-MM-DD` header to `sbom_diff.md` generation script
- [x] Update CI to stamp the date dynamically

### L16-12 — SBOM Cosign Signing

- [x] Sign `pg_ripple.cdx.json` with `cosign` in `release.yml` workflow
- [x] Signed artifact available on GitHub release page
- [x] Document verification procedure in `RELEASE.md`

### L16-13 — Release Checklist Version Bump Note

- [x] Add bullet to `RELEASE.md`:
  "Run `just bump-version-dry` then `just bump-version <new> <floor>` and verify the compat constant before tagging"

### L16-14 — SSE Concurrency Tests

- [x] Add `tests/concurrency/sse_burst_subscriber.sh`:
  - 100 simultaneous SSE connections
  - Assert no dropped events under burst
- [x] Add `tests/concurrency/sse_reconnect_during_merge.sh`:
  - Reconnect during merge worker cycle
  - Assert no gap in event stream

### L16-15 — AGENTS.md Link in CONTRIBUTING.md

- [x] Add link to `AGENTS.md` in `CONTRIBUTING.md`

### M16-12 — Entity Resolution Concurrency Tests

- [x] Add `tests/concurrency/entity_resolution_concurrent_resolves.sh`
- [x] Add `tests/concurrency/temporal_versioned_write_race.sh`

### M16-13 — Additional Fuzz Targets

- [x] Add `fuzz/fuzz_targets/temporal_query.rs`
- [x] Add `fuzz/fuzz_targets/pprl_bloom_encode.rs`
- [x] Add `fuzz/fuzz_targets/rule_authoring_validate.rs`
- [x] Add `fuzz/fuzz_targets/skos_bundle.rs`
- [x] Register all new fuzz targets in `fuzz/Cargo.toml`

### Migration & Release

- [x] Migration script `sql/pg_ripple--0.116.0--0.117.0.sql` (comment-only: no schema changes)
- [x] `pg_ripple.control` updated to `default_version = '0.117.0'`
- [x] `Cargo.toml` and `pg_ripple_http/Cargo.toml` versions bumped to `0.117.0`
- [x] `COMPATIBLE_EXTENSION_MIN` bumped to `"0.116.0"` in `pg_ripple_http/src/main.rs`
- [x] `CHANGELOG.md` entry present for `[0.117.0]`

## Exit Criteria

- All checklist items above are ticked
- `cargo clippy --features pg18 -- -D warnings` passes with zero warnings
- `cargo pgrx test pg18` passes
- `cargo pgrx regress pg18` passes
- `#[allow(...)]` count ≤ 186
- All build artifacts removed from repository and added to `.gitignore`
- Migration script `sql/pg_ripple--0.116.0--0.117.0.sql` applies cleanly
- `CHANGELOG.md` entry present for `[0.117.0]`
